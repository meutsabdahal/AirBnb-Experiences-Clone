import logo from "../img/airbnb-logo.png"

function NavBar() {
    return (
        // adding images to nav bar 
        <nav>
            <img src = {logo} />
        </nav>
    )
}

export default NavBar